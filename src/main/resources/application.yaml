spring:
  datasource:
    url: jdbc:mysql://localhost:3306/conduitspring
    username: conduitspring
    password: conduitspring
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate.format_sql: true
server:
  port: 4567